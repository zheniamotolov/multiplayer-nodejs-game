export default class Player{
constructor(initializationPackage){
    this.id=initializationPackage.id;
    this.number=initializationPackage.number;
    this.x=initializationPackage.x;
    this.y=initializationPackage.y;
}

};